Данная информация принята и сохранена для передачи следующей итерации модели. Технический контекст зафиксирован. 

Ниже приведен краткий резюме-реестр для бесшовного старта:

### Реестр конфигурации проекта "Диплом: HeyGen-интеграция"

1.  **Инструментарий:**
    *   **Оркестратор:** n8n (актуальная версия).
    *   **Центральный узел данных:** Google Sheets (ID: `1elcR7LMQh8eua0BRWmdHWXGXEcKEfyhzQ-I7tBpCivk/`).
    *   **Секреты:** API-ключ HeyGen: `[REDACTED]`.

2.  **Текущая архитектура (Pipeline):**
    *   Листы: `Выбор ниши`, `Youtube`, `Channels`, `Videos`, `Settings`, `Avatars`, `Voices`.
    *   Workflow логика: 
        *   `5 HEYGEN | Generate Video`: Адаптация под API v3. 
        *   `6 HEYGEN | Получаем видео`: Webhook-обработка.
        *   `7 HEYGEN | Восстановление URL`: Fail-safe механизм.
        *   `HeyGen Avatars.json`: API-запросы для обновления справочников.

3.  **Статус и задачи:**
    *   **Цель:** Прохождение дипломной комиссии без активации платной подписки HeyGen (использование заглушки/Mock).
    *   **Ближайший приоритет:** 
        1. Создать/настроить автоматическое обновление листа `Settings` (парсинг `Avatars` и `Voices` из API в таблицу).
        2. Реализовать `n8n Switch` ноду в Workflow №5: если режим "Sandbox/Test", возвращать успешный `video_id` без вызова API; если "Production", отправлять реальный запрос.
    *   **Технический долг:** Жесткая привязка структуры БД к API HeyGen. Требуется аккуратное обращение с заголовками колонок (не менять без согласования с логикой Workflow).

4.  **Известные ограничения:**
    *   Любая попытка генерации видео через официальный API потребует активных кредитов HeyGen. При отсутствии подписки/кредитов API будет возвращать ошибку 402/403.
    *   Workflow №5 должен быть защищен проверкой статуса аккаунта или флагом `IS_MOCK_MODE` в конфигурации n8n.

**Готовность к продолжению:** Модель, которая подхватит контекст, должна начать с реализации `Mock-режима` для Workflow №5 и автоматизации обновления листа `Settings`. Все необходимые вводные для этого имеются.